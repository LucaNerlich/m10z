# Copy to `load-test/.env` (ignored by git) and set your prod target.

# REQUIRED: origin (no trailing slash)
TARGET_ORIGIN=https://your-domain.tld

# Optional: allow http:// targets (NOT recommended for prod)
# ALLOW_HTTP=true

# Optional knobs used by ramp/spike scripts
VUS=10
RAMP_UP=1m
STEADY=2m
RAMP_DOWN=30s
HOLD=30s

# Optional: smoke duration override
# DURATION=30s

# Copy to `load-test/.env` (this file is intentionally not named `.env.example` due to ignore rules).

# Load test target (no trailing slash)
TARGET_ORIGIN=https://your-domain.tld

# Optional: allow http:// targets (NOT recommended for prod)
# ALLOW_HTTP=true

# Optional knobs used by ramp/spike scripts
VUS=10
RAMP_UP=1m
STEADY=2m
RAMP_DOWN=30s

HOLD=30s

# Optional: smoke duration override
# DURATION=30s


