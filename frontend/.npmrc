# Quarantine newly published packages for 7 days (10080 minutes) before installation.
# Malicious packages are typically detected and removed within days of release.
minimum-release-age=10080

# Block subdependencies from using git: URLs or remote tarball specifiers,
# which would pull code from arbitrary sources bypassing the registry entirely.
block-exotic-subdeps=true
